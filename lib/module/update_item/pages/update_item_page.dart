import 'package:flutter/material.dart';

class UpdateItemPage extends StatelessWidget {
  const UpdateItemPage({super.key});


  @override
  Widget build(BuildContext context) {

    return Scaffold(
    appBar: AppBar(title: const Text('UpdateItemPage')),

    body: const Text('UpdateItemPage'),
    );
  }
}